# Top-Level Control Propagation Patch Spec, 2026-04-30

Agent: 328.
Scope: report-only attack-heal specification.  No manuscript TeX, scripts,
figures, bibliography, PDF, source fixtures, `CLAUDE.md`, or `AGENTS.md` were
edited.

## Claim Attacked

After Agent 324, future agents might still load only the top-level control
files and miss the late 2026-04-30 theorem-control predicates:

- native \(\C^2\) holomorphic \(E_2\) taxonomy, not curve VOA import;
- BMK one-pair pro-Matlis split and
  \(\operatorname{Ob}^{\Pi}_{\mathrm{BM}}\);
- \(\theta_3\) row, Bianchi defect, and
  \(\Delta^1_{M,N}\);
- radial \(\Omega^{\mathrm{rad}}\), decorated PBW Stokes, and signed-row
  obstruction;
- non-scalar QME finite-window matrix criterion;
- Vol II `chriss-ginzburg-rectify` manuscript-proper mandate.

## Verdict

The theorem surfaces are mostly current.  `main.tex`, `theorem-lanes.tex`,
`open-obligations.tex`, and the detailed portions of `claim-strength-ledger.tex`
already carry the native \(E_2\), BMK, \(\theta_3\), radial, and non-scalar QME
gates.

The top-level control layer remains partial.  `CLAUDE.md` and `AGENTS.md`
still name the local \(\C^2\) surface, compact-CY firewall, curve-VOA
reduction gate, Omega discipline, and Supremum Discipline, but do not name the
BMK/pro-Matlis, \(\theta_3/\Delta^1\), radial signed-row, finite-window QME
matrix, or Vol II `chriss-ginzburg-rectify` mandate.  The snapshot
`reconstitution/attack-heal-latest-2026-04-30.md` is also stale in its
obstruction list.

One TeX summary remains stale: `claim-strength-ledger.tex:1263-1276` still
summarizes the radial branch by the older \(\mathfrak o^{\mathrm{rad}}\) /
\(\mathcal C_{a,b}\) / uniform contracting-homotopy language without the
sharper \(\Omega^{\mathrm{rad}}\) / decorated Stokes / signed-row form.
This is a deferred TeX patch target only; no TeX edit was made.

## Evidence Anchors

- Top-level native \(\C^2\) and curve-VOA firewall:
  `CLAUDE.md:39-56`; `AGENTS.md:38-39`, `AGENTS.md:89-96`.
- Top-level Omega but not QME warning:
  `CLAUDE.md:58-75`; `AGENTS.md:97-103`.
- Top-level Supremum Discipline:
  `CLAUDE.md:119-141`; `AGENTS.md:105-117`.
- Native holomorphic \(E_2\) theorem surface:
  `theorem-lanes.tex:381-388`, `theorem-lanes.tex:409-417`;
  `open-obligations.tex:224-254`;
  `claim-strength-ledger.tex:647-690`.
- BMK/pro-Matlis split and obstruction:
  `theorem-lanes.tex:519-533`;
  `main.tex:1260-1474`, `main.tex:1512-1540`, `main.tex:1693-1704`,
  `main.tex:2565-2579`;
  `open-obligations.tex:274-309`;
  `claim-strength-ledger.tex:319-337`, `claim-strength-ledger.tex:647-690`.
- \(\theta_3\), Bianchi defect, and \(\Delta^1\):
  `main.tex:8817-9022`;
  `theorem-lanes.tex:3448-3591`;
  `open-obligations.tex:793-955`;
  `claim-strength-ledger.tex:921-991`.
- Radial \(\Omega^{\mathrm{rad}}\), decorated PBW Stokes, signed row:
  `main.tex:1069-1080`, `main.tex:2425-2462`, `main.tex:7446-7473`,
  `main.tex:8305-8348`;
  `theorem-lanes.tex:2537-2565`, `theorem-lanes.tex:3670-3704`;
  `open-obligations.tex:498-529`, `open-obligations.tex:1404-1440`;
  `claim-strength-ledger.tex:207-216`, `claim-strength-ledger.tex:301-309`,
  `claim-strength-ledger.tex:578-590`, `claim-strength-ledger.tex:993-1049`.
- Non-scalar QME finite-window criterion:
  `open-obligations.tex:545-705`, `open-obligations.tex:1138-1220`;
  `main.tex:2451-2462`, `main.tex:8796-8814`, `main.tex:9037-9050`;
  `theorem-lanes.tex:2703-2865`, `theorem-lanes.tex:3406-3606`;
  `claim-strength-ledger.tex:921-991`, `claim-strength-ledger.tex:1290-1348`,
  `claim-strength-ledger.tex:1406-1413`.
- Vol II rectification mandate exists below top-level:
  `.agents/skills/chriss-ginzburg-rectify/SKILL.md:1-14`;
  `reconstitution/critique-refresh-2026-04-30-0957-ingestion-and-swarm-plan.md:198-203`;
  `reconstitution/attack-heal-216-launch-manifest-2026-04-30.md:56-59`.

## Exact Minimal Patch Targets

No patch below was applied.

### 1. `CLAUDE.md:56`

Insert after the "Native versus reduced chiral structures" paragraph:

```markdown
**Late 2026-04-30 theorem-control predicates.** Future theorem work must
retain the native \(\mathbb C^2\) holomorphic \(E_2\) taxonomy before any
curve-chiral reduction.  The BMK lane separates the one-pair analytic
pro-Matlis retract from the strict native all-window support-local current
transfer, whose obstruction is
\(\operatorname{Ob}^{\Pi}_{\mathrm{BM}}\).  The first certified larger
non-scalar row is \(\theta_3\): a named \(C_{\theta_3}\) is evidence only
with a CE ancestor, a scalar-zero Costello local counterterm, or a complete
companion-face table, and tower compatibility passes through
\(\Delta^1_{M,N}=-\pi_{M,N}\mathfrak b^M+\mathfrak b^N\) plus the secondary
\(\varprojlim^1H^0\) primitive class.  The radial/Weyl theorem surface is
\(\Omega^{\mathrm{rad}}_{a,b}\), equivalently decorated PBW Stokes for
\(D^\square_{a,b}\), with failure exactly a signed row in
\(\ker B_{a,b}^*\).  The larger non-scalar Costello/QME theorem requires
the filtered scalar projection, finite row arrays, the primitive matrix
\(A^Mc=-r^M\), transition matrices, Roos compatibility, centrality
homotopies, and a curved bulk-to-defect kernel.
```

### 2. `CLAUDE.md:102`

Insert at the start of the "Long-form proof harness" operational paragraph:

```markdown
For manuscript-proper writing, rewriting, or theorem-lane reconstitution,
load and follow
`~/chiral-bar-cobar-vol2/.agents/skills/chriss-ginzburg-rectify/SKILL.md`
before writing.  Its unique-survivor, instant-computation, and hostile
Beilinson-audit loop is mandatory under this repository's Supremum
Discipline.
```

### 3. `AGENTS.md:104`

Insert after current rule 10 and renumber the present Supremum rule from
11 to 13:

```markdown
11. The late 2026-04-30 theorem-control predicates are binding before
    theorem work: native `\mathbb C^2` holomorphic `E_2` taxonomy before
    curve reduction; BMK one-pair pro-Matlis retract is not strict native
    all-window support-local current transfer, whose obstruction is
    `\operatorname{Ob}^{\Pi}_{\mathrm{BM}}`; `\theta_3` requires a CE
    ancestor, scalar-zero Costello counterterm, or complete companion-face
    table plus `\Delta^1_{M,N}` and secondary `\varprojlim^1H^0`;
    radial/Weyl means the `\Omega^{\mathrm{rad}}` / decorated PBW Stokes /
    signed-row obstruction surface; larger non-scalar QME means scalar
    projection, finite row arrays, `A^Mc=-r^M`, transition matrices, Roos
    compatibility, centrality homotopies, and a curved bulk-to-defect
    kernel.
12. For manuscript-proper writing, rewriting, or theorem-lane
    reconstitution, load and follow
    `~/chiral-bar-cobar-vol2/.agents/skills/chriss-ginzburg-rectify/SKILL.md`
    before editing.
```

The same factual insertion must be mirrored in `CLAUDE.md` in the same
integration commit because `AGENTS.md:5` requires no factual divergence.

### 4. `reconstitution/attack-heal-latest-2026-04-30.md:89`

Insert after "Retired Stale Framing" or before "Evidence Window" a load
mandate visible to snapshot-only agents:

```markdown
## Top-Level Control Load Mandate

Future agents must load `CLAUDE.md`, `AGENTS.md`, this snapshot, and, before
any manuscript-proper writing or theorem-lane reconstitution,
`~/chiral-bar-cobar-vol2/.agents/skills/chriss-ginzburg-rectify/SKILL.md`.
The binding late predicates are the native \(\C^2\) holomorphic \(E_2\)
taxonomy; BMK one-pair pro-Matlis retract versus
\(\operatorname{Ob}^{\Pi}_{\mathrm{BM}}\); \(\theta_3\) with
\(\Delta^1_{M,N}\) and secondary \(\varprojlim^1H^0\); radial
\(\Omega^{\mathrm{rad}}\), decorated PBW Stokes, and signed-row obstruction;
and the non-scalar QME finite-window matrix/Roos/curved-kernel criterion.
```

### 5. `reconstitution/attack-heal-latest-2026-04-30.md:181-208`

Replace the stale "Precise Obstruction Classes" list entries with:

```markdown
- BMK native all-window current obstruction:
  \[
    \operatorname{Ob}^{\Pi}_{\mathrm{BM}}
    =
    (\operatorname{ob}_{\oplus},\operatorname{ob}_{I_\Pi},
     \operatorname{ob}_{\mathfrak h,\Pi},
     \operatorname{ob}_{\mathrm{collar},\Pi},
     \operatorname{ob}_{3,\Pi},\operatorname{ob}_{\mathrm{unif},\Pi}).
  \]
- Non-scalar graph/QME fixed-window obstruction:
  \(A^Mc=-r^M\), with failure certified by
  \(\ell A^M=0\), \(\ell(r^M)\ne0\); inverse-limit closure requires
  transition matrices and vanishing of
  \(\mathfrak O^{\mathrm{ns}}_n=(([\mathfrak o^{\mathrm{ns}}_{n,M}])_M,
  \lambda_n)\).
- \(\theta_3\) Bianchi transport:
  \[
    \Delta^1_{M,N}=-\pi_{M,N}\mathfrak b^M+\mathfrak b^N,
  \]
  followed by the secondary \(\varprojlim^1H^0\) primitive-compatibility
  class after a primitive is chosen.
- Radial all-\(N\) trace obstruction:
  \[
    \Omega^{\mathrm{rad}}_{a,b}
      =[(T_{a,b},E^+_{a,b})]\in\operatorname{coker}B_{a,b},
  \]
  equivalently decorated PBW Stokes
  \(D^\square_{a,b}=C^+_{a,b}\partial_2\); failure is exactly a signed row
  \((\phi,-\lambda)\in\ker B_{a,b}^*\) with
  \(\lambda(E^+_{a,b})-\phi(T_{a,b})\ne0\).
```

Keep the existing wavefront, arbitrary \(\mathcal D'_c(I)\), scalar-projection,
and vertex/OPE obstruction bullets if they are still needed; they are not
stale.

### 6. `claim-strength-ledger.tex:1263-1276`

Deferred TeX target.  Replace the older
\(\mathfrak o^{\mathrm{rad}}\) / \(\mathcal C_{a,b}\) / uniform homotopy
summary by the sharper obstruction surface already used elsewhere:

```tex
The sharper free-diagram and dual-potential form is the obstruction
\[
  \Omega^{\mathrm{rad}}_{a,b}
  =
  [(T_{a,b},E^+_{a,b})]\in\operatorname{coker}B_{a,b},
\]
equivalently decorated PBW Stokes
\(D^\square_{a,b}=C^+_{a,b}\partial_2\).  Finite certificates prove
vanishing in the recorded windows and exhibit the nontrivial correction
\(K_{4,4}\); failure of the all-bidegree theorem is exactly a signed row
\((\phi,-\lambda)\in\ker B_{a,b}^*\) with
\(\lambda(E^+_{a,b})-\phi(T_{a,b})\ne0\).
```

Do not touch `open-obligations.tex:498-529`, `main.tex`, or
`theorem-lanes.tex` for this control goal; they already carry the current
radial language.  Do not edit any TeX until explicitly instructed.

## Checks Run

- Read `CLAUDE.md`, `AGENTS.md`,
  `reconstitution/attack-heal-latest-2026-04-30.md`,
  `reconstitution/vol2-control-surface-propagation-audit-2026-04-30.md`,
  Agent 324's swarm report, the local and Vol II
  `chriss-ginzburg-rectify` skill files, and current theorem surfaces.
- Used `rg` and `nl -ba` to verify the live anchors above.
- Ran `git status --short` and observed concurrent dirty state.
- No build was run; this was report-only.

## Files Changed By Agent 328

- `reconstitution/top-level-control-propagation-patch-spec-2026-04-30.md`
- `reconstitution/swarm-2026-04-30-agent-328-top-level-control-propagation-patch-spec.md`
