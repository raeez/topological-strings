# Agent 317 Non-scalar QME Frontier Integration Spec

Date: 2026-04-30.
Status: report-only. No manuscript TeX, script, figure, source, or build file
was edited.

Owned integration report:
`reconstitution/nonscalar-qme-frontier-integration-spec-2026-04-30.md`.

## Claim Attacked

The attacked claim was that Agent 311's finite-window local-functional
obstruction criterion and minimal full-equivariant zero branch close the full
non-scalar Costello graph/QME theorem.

That claim is false. The criterion is theorem-grade. The minimal
full-equivariant branch is theorem-grade in its named habitat. The larger
Costello graph/QME realization is still a construction target.

## Evidence Read

Primary local anchors read:

- `CLAUDE.md`; `AGENTS.md`; `~/ecosystem/INVARIANTS.md`;
  `~/ecosystem/AGENTS-HARNESS.md`.
- `main.tex:8320-8991`.
- `theorem-lanes.tex:3334-3572`.
- `open-obligations.tex:505-915`.
- `claim-strength-ledger.tex:104-122`, `216-234`, `496-546`,
  `1292-1339`.
- `appendix-unreduced-bv-qme.tex:2142-2385`, `2873-3314`.
- `scripts/finite_window_graph_array.py`.
- Agent reports 291, 297, and 311, including their detailed reports in
  `reconstitution/`.

## Result

The proved theorem is the finite-window obstruction criterion in the
restricted local-functional habitat:
\[
  \mathfrak Q^\bullet_{\mathcal G,\Lambda,M}(I)
  =
  \left(
  \mathcal O^{\mathrm{bd}}_{\mathrm{loc},\mathcal G,\Lambda,M}
  (\mathcal E_w^M|_I;A^{\mathrm{pp},w,M}_{\partial,\hbar}(I)),
  d_M=Q+\{I^w_{0,M},-\}_{\hbar,M}
  \right),
\]
with labels restricted to regular densities, graphwise wavefront-admissible
tuples, or sign-conic finite-scaling classes.

The Hom differential is
\[
  d_{\mathbf K,M}T=d_MT-(-1)^{|T|}T\,d_{\mathrm{CE}},
\]
and the curvature is
\[
  \operatorname{Curv}_{\mathbf K,M}(\kappa)
  =
  d_{\mathbf K,M}\kappa+\frac12[\kappa,\kappa]_{\mathbf K,M}.
\]
The source-face term is part of the QME differential.

The non-scalar complex is formed only after constructing
\[
  \widehat\sigma_{\mathrm{sc},M}\colon
  \mathfrak Q^\bullet_{\mathcal G,\Lambda,M}(I)
  \to
  C^\bullet_{\mathrm{Lie}}(\bar A;\C\gamma_{\mathbf 1})[1][[\hbar]],
\]
with
\[
  \mathcal K^\bullet_{\mathrm{ns},M}(I)
  =
  \ker\widehat\sigma_{\mathrm{sc},M}.
\]

The row criterion is:
\[
  \mathfrak s_{n,M}
  =
  \sum_{\alpha\in\mathsf A^{\mathrm{fw}}_{n,M}}
  \widehat\sigma_{\mathrm{sc},M}(R^{\mathrm{row}}_{\alpha,M})=0,
\]
then
\[
  R^{\mathrm{ns}}_{n,M}=\sum_i r_i^M\rho_i^M,\qquad
  d_{\mathrm{ns},M}\eta_j^M=\sum_iA^M_{ij}\rho_i^M,
\]
and a fixed-window primitive exists exactly when
\[
  A^Mc_M=-r^M.
\]
The tower additionally requires
\[
  T^{M,N}r^M=r^N,\qquad
  T^{M,N}A^M=A^NP^{M,N},\qquad
  [P^{M,N}c_M-c_N]=0.
\]

## Positive Branch

The minimal full-equivariant branch is proved in the habitat generated by the
strict current-interface rows, the one-cross scalar-contact row
\(\alpha_{\mathrm{sc}}\), and subset-deletion closure, with no added
positive-order seed, CE-source face, graph-counterterm face, or nonzero
scalar-zero row. For every \(n\ge1\),
\[
  R^{\mathrm{ns}}_{n,M}=0,\qquad
  \mathfrak o^{\mathrm{ns}}_{n,M}=0,\qquad
  C_{n,M}=0.
\]
The binary row is
\[
  A^M_{\min}:0\to0,\qquad
  r^M_{\min}=0,\qquad
  h^M_{\min}=0,
\]
with zero Roos class.

## Obstruction Classes

The first supplied larger-row obstruction is theta3:
\[
  \mathsf E_{\theta_3,M}
  =
  -K^M_{\Theta_3}(\zeta_{M,\nu_3}),\qquad
  \mathcal Q^0_{\theta_3,M}=0,\quad
  \mathcal Q^1_{\theta_3,M}=\C\mathsf E_{\theta_3,M},\quad d_M=0.
\]
Thus
\[
  [\mathsf E_{\theta_3,M}]\ne0,\qquad
  \ell_{\theta_3}(\mathsf E_{\theta_3,M})=1.
\]
The current matrix is
\[
  A^M_{\theta_3}=[\,]_{1\times0},\qquad r^M=(1).
\]
The direct repair would be
\[
  A^M_{\theta_3,C}=(-1),\qquad
  d_MC_{\theta_3,M}=-\mathsf E_{\theta_3,M},
\]
with scalar-zero value, locality, \(TA=AP\), and zero Roos class.

The ordinary pure two-\(u\) source route is blocked by
\[
  q_{2u}
  =
  e^*_{((H_{0,1},H_{2,0}),c_{\rho_{2,1}})}
  -\frac12
  e^*_{((H_{0,1},H_{0,1}),c_{\rho_{0,2}})},
\]
\[
  q_{2u}d_{\mathrm{CE}}=0,\qquad
  q_{2u}(\zeta_{M,\nu_3})=1.
\]
This is only a no-go for that source envelope.

The lower Bianchi obstruction is
\[
  \mathfrak b^2_{02,20}
  =
  d_{\mathrm{ns},2}K^2_{\Theta_3}(\zeta^0_2)
  -
  K^2_{\Theta_3}(d_{\mathrm{CE}}\zeta^0_2),
\]
where
\[
  \zeta^0_2=u_{a,H_{1,0}}u_{b,H_{0,1}},\qquad
  d_{\mathrm{CE}}\zeta^0_2=2\zeta_{\nu_{02}}-2\zeta_{\nu_{20}}.
\]
In the basis
\[
  (E^2_{\nu_{02}},E^2_{\nu_{20}},\mathfrak b^2_{02,20}),
\]
the available column and residual are
\[
  A_D^2=(-2,2,1)^T,\qquad r_2=(2,-2,0)^T.
\]
The cokernel
\[
  \widetilde\lambda_{02,\mathfrak b}
  =
  \frac12(E^2_{\nu_{02}})^*
  +(\mathfrak b^2_{02,20})^*
\]
satisfies
\[
  \widetilde\lambda_{02,\mathfrak b}A_D^2=0,\qquad
  \widetilde\lambda_{02,\mathfrak b}(r_2)=1.
\]
The missing Bianchi-killing column is
\[
  A_B^2=(0,0,-1)^T,\qquad
  d_{\mathrm{ns},2}B^2_{02,20}
  =
  -\mathfrak b^2_{02,20}.
\]
The tower obstruction is
\[
  \Delta^1_{M,N}
  =
  -\pi_{M,N}\mathfrak b^M+\mathfrak b^N,
\]
which must be a \(d_{\mathrm{ns}}\)-boundary and then have zero secondary
\(\varprojlim^1H^0\) class.

## Exact Integration Needed

No TeX edit is required to fix theorem strength before authorization. The
current manuscript already fences the full Costello graph/QME theorem as an
obstruction/construction target. If editing is authorized, the integration
should be surgical:

- `main.tex:8488-8722`: normalize to the restricted
  \(\mathfrak Q^\bullet_{\mathcal G,\Lambda,M}(I)\) habitat and explicitly
  exclude arbitrary \(\mathcal D'_c(I)\) graph kernels.
- `main.tex:8724-8991`: retain theta3 as an obstruction theorem; add only the
  accepted direct column, marked-source, companion-table, or
  \(B^2_{02,20}\) healing data.
- `theorem-lanes.tex:3334-3572`: keep the branch catalogue finite-window and
  row-data based; do not promote the eight-face source row.
- `open-obligations.tex:505-915`: consolidate the construction tuple
  \[
    (\mathcal G_M^{\mathrm{mk}},
     \widehat\sigma_{\mathrm{sc},M},
     \kappa_{\hbar,w,I},
     R^{\mathrm{row}}_{\alpha,M},
     A^M,T^{M,N},P^{M,N},\lambda_n)
  \]
  plus one accepted theta3 or Bianchi heal.
- `claim-strength-ledger.tex:496-546`: keep the larger non-scalar
  Costello/QME package as a construction target with exact obstruction pair.
- `appendix-unreduced-bv-qme.tex:2142-2385`: normalize the
  \(\Lambda\)-restricted habitat notation if the main text is changed.

## Verification

Ran:

```bash
python3 -m py_compile scripts/finite_window_graph_array.py
python3 scripts/finite_window_graph_array.py
```

Observed floor:

```text
minimal_full_equivariant_order_2_zero: primitive_exists true; C_{2,M}=0; Roos class 0.
theta_3_current_finite_row_subcomplex: primitive_exists false; residual E_theta_3; left covector value 1.
theta3 exact payload fixtures: accepted only when dC=-E, scalar projection zero, T A = A P, and Roos class 0 are supplied.
theta3 eight-face candidate: rejected; diagonal N=2 transport leaves 2E_nu02-2E_nu20 and the marked Costello incidence/weight table is absent.
```

No PDF build was run because this was report-only.
