# Vol II Control Surface Propagation Audit, 2026-04-30

Agent: 324.
Scope: report-only attack-heal audit.  No manuscript TeX, scripts,
figures, bibliography, PDF, or source fixtures were edited.

## Inputs Read

- `CLAUDE.md`
- `AGENTS.md`
- `~/ecosystem/INVARIANTS.md`
- `~/ecosystem/AGENTS-HARNESS.md`
- `~/chiral-bar-cobar-vol2/.agents/skills/chriss-ginzburg-rectify/SKILL.md`
- Vol II control/cache files:
  `notes/antipatterns_catalogue.md`,
  `notes/first_principles_cache.md`,
  `notes/first_principles_cache_comprehensive.md`,
  `notes/vol1_control_surface_import_manifest_2026_04_30.md`,
  `notes/vol1_imported_antipatterns_catalogue_2026_04_30.md`,
  `notes/vol1_imported_first_principles_cache_comprehensive_2026_04_30.md`
- This repo's control layer:
  `open-obligations.tex`, `claim-strength-ledger.tex`,
  `reconstitution/attack-heal-latest-2026-04-30.md`, and the relevant
  2026-04-30 reconstitution audits for native `E_2`, BMK/Matlis,
  `\theta_3`, radial/Weyl, and non-scalar QME.

## Verdict

Propagation is strong in `open-obligations.tex` and
`claim-strength-ledger.tex`.  The major Vol II anti-patterns are now visible
as theorem gates, obstruction vectors, or false-transfer firewalls.

Propagation is partial in the top-level control files `CLAUDE.md` and
`AGENTS.md`.  They correctly block compact-CY/CoHA/BKM and curve-VOA false
transfer, state Supremum Discipline, and identify the native `\C^2`
holomorphic `E_2` surface.  They do not yet name the newer cache predicates:
BMK pro-Matlis split, `\theta_3`/`\Delta^1`, radial `\Omega^{rad}`, and the
finite-window non-scalar QME matrix criterion.

The internal snapshot `reconstitution/attack-heal-latest-2026-04-30.md` is
stale relative to later 2026-04-30 updates.  It records the correct local
firewall and non-scalar graph/QME class, but its radial obstruction entry is
older `K_{a,b}` language and it omits the BMK pro-Matlis split and
`\theta_3` Bianchi transport class.

## Vol II Source Control Checked

Vol II's imported topological-strings cache rows are explicit at
`~/chiral-bar-cobar-vol2/notes/first_principles_cache_comprehensive.md:10962-11035`
and `:11039-11104`.

The binding rows for this repo are:

- AP2172: polynomial one-psi labels are not Matlis principal parts.
- AP2174: strict product/direct-sum endpoints do not provide a Costello
  BV coefficient kernel.
- AP2175: reduced current maps do not construct the unreduced `P_0`
  factorization center.
- AP2176: first/third Moyal checks do not prove all-order Costello
  graph/QME realization.
- AP2177 and AP2188: local Hamiltonian BF has no compact-CY, global BCOV,
  Vol III, Igusa/BKM, MNOP, or sibling-volume consequence without matched
  target datum, obstruction vector, and null-homotopies.
- AP2184: Vol II supplies `SC^{ch,top}` grammar, not a theorem import.
- AP2185: BV/QME claims require the Costello habitat and the obstruction
  complex carrying `Ob_{n,nsc}`.
- AP2190: `\mathbf H_{\Delta_5}` is a gated K3 comparator, not a native
  Vol II output.

## Surface Audit

| Control predicate | `CLAUDE.md` / `AGENTS.md` | `open-obligations.tex` | `claim-strength-ledger.tex` | Status |
|---|---|---|---|---|
| Compact-CY / CoHA / BKM / curve-VOA false transfer | Present.  `CLAUDE.md:31-56`, `:77-88`; `AGENTS.md:60-64`, `:83-96`, `:258-269`, `:281-282`. | Present.  `open-obligations.tex:9-13`, `:224-254`, `:1462-1501`. | Present.  `claim-strength-ledger.tex:57-63`, `:241-247`, `:1410-1432`. | Propagated. |
| Supremum Discipline | Present.  `CLAUDE.md:119-141`; `AGENTS.md:105-117`, `:139-148`. | Present locally at `open-obligations.tex:805-807` and `:1431-1437`. | Present as controller at `claim-strength-ledger.tex:632-638`. | Propagated. |
| Native `\C^2` holomorphic `E_2` taxonomy | Present.  `CLAUDE.md:39-56`; `AGENTS.md:31-49`, `:89-96`. | Present as a gate at `open-obligations.tex:224-254` and through Matlis/BMK rows at `:256-309`. | Present at `claim-strength-ledger.tex:319-351` and `:641-705`. | Propagated, with minor top-level compression. |
| BMK pro-Matlis split | Not named. | Present at `open-obligations.tex:274-309`. | Present at `claim-strength-ledger.tex:319-336` and `:641-684`. | Propagated below top-level; add top-level control row. |
| `\theta_3` / `\Delta^1` Bianchi transport | Not named. | Present at `open-obligations.tex:790-952`, especially `:939-950`. | Present at `claim-strength-ledger.tex:512-546` and `:932-985`. | Propagated below top-level; add top-level control row. |
| Radial obstruction surface | Only generic `radial-parts` wording. | Detailed gate present at `open-obligations.tex:1410-1437`.  Earlier summary at `:498-526` still uses older `\mathcal C_{a,b}` language. | Detailed gate present at `claim-strength-ledger.tex:987-1043`.  Several summary rows still use older `\mathcal C_{a,b}` or homotopy language. | Partial propagation. |
| Non-scalar QME obstruction criterion | Generic warning only. | Present at `open-obligations.tex:542-700`, `:704-788`, and `:1113-1217`. | Present at `claim-strength-ledger.tex:915-985` and `:1295-1407`. | Propagated below top-level; add top-level control row. |

## Attack-Heal Findings

### A324-01: Top-level control files omit late cache predicates

Attack: a new agent who reads only `CLAUDE.md` and `AGENTS.md` sees the
local `\C^2` and compact-CY/curve-VOA firewalls, but not the late-day
BMK, `\theta_3`, radial, and non-scalar QME control predicates.

Heal: `open-obligations.tex` and `claim-strength-ledger.tex` contain the
predicates.  The top-level files should receive a compact "late control
predicates" paragraph so future agents do not have to rediscover them from
ledger TeX.

Status: partial propagation, no theorem failure.

### A324-02: BMK pro-Matlis is now visible where it matters

Attack: Vol II AP2174/AP2175 blocks strict endpoint and reduced-current
overclaims.  The repo must distinguish the positive one-pair analytic
pro-Matlis retract from the native support-local all-window current theorem.

Heal: current `open-obligations.tex:274-309` states the positive retract
and the six-entry `Ob^{\Pi}_{BM}`.  `claim-strength-ledger.tex:319-336`
and `:641-684` repeat the split.  This fixes the earlier missing
open-obligation row reported by older BMK audits.

Residual: top-level control files do not name the split.

### A324-03: `\theta_3` and `\Delta^1` are visible after later updates

Attack: Agent 309 found `\Delta^1_{M,N}` missing or unnamed in several
surfaces.  The current checkout must be rechecked, not judged from that
older report.

Heal: `open-obligations.tex:939-950` and
`claim-strength-ledger.tex:972-983` now name
`\Delta^1_{M,N}` and the secondary `\varprojlim^1H^0` gate.  The fixed
lower matrix and missing Bianchi-killing column are visible at
`open-obligations.tex:886-936` and `claim-strength-ledger.tex:949-969`.

Residual: top-level control files and `attack-heal-latest` do not name
the class.

### A324-04: Radial obstruction surface is present, but summary rows lag

Attack: after Agent 314/320, the radial/Weyl surface should be the exact
obstruction class
`Omega^{rad}_{a,b}=[(T_{a,b},E^+_{a,b})] in coker B_{a,b}`, with failure
a signed row in `ker B_{a,b}^*` and the positive theorem equivalent to
decorated PBW Stokes for `D^\square_{a,b}`.

Heal: the detailed open-obligation surface has this at
`open-obligations.tex:1410-1437`; the detailed claim ledger has it at
`claim-strength-ledger.tex:987-1043`.

Residual: `open-obligations.tex:498-526`,
`claim-strength-ledger.tex:207-215`, `:300-308`, `:577-585`, and
`:1238-1270` still summarize the radial target with older
`\mathcal C_{a,b}` / contracting-homotopy language.  This is a
propagation lag, not a mathematical contradiction.

### A324-05: Non-scalar QME criterion is visible below top-level

Attack: Vol II AP2185 forbids treating habitat names, Moyal coefficients,
or minimal zero branches as a full Costello graph/QME theorem.

Heal: `open-obligations.tex:542-700` states the finite-window row types,
scalar gate, non-scalar residual, primitive matrix `A^M c=-r^M`, left
cokernel certificate, transition matrices, and Roos obstruction pair.
`claim-strength-ledger.tex:915-985` states the same criterion with the
Costello curvature equation and the `\theta_3` model row.  The minimal
full-equivariant branch is isolated from larger graph packages at
`claim-strength-ledger.tex:1295-1342`.

Residual: top-level files still warn only generically that Omega data do
not prove non-scalar QME centrality.

## Exact Patch Targets

No patch below was applied.

### 1. `CLAUDE.md:56`

Insert after the "Native versus reduced chiral structures" paragraph:

```markdown
**Late 2026-04-30 control predicates.**  The Vol II cache is admissible
here only as an anti-pattern controller.  The BMK lane separates the
one-pair analytic pro-Matlis retract from the strict native all-window
support-local current theorem, whose obstruction is
`\operatorname{Ob}^{\Pi}_{\mathrm{BM}}`.  The first certified larger
non-scalar row is the `\theta_3` row: a named `C_{\theta_3}` is evidence
only with a CE ancestor, a scalar-zero Costello local counterterm, or a
complete companion-face table, and tower compatibility passes through
`\Delta^1_{M,N}=-\pi_{M,N}\mathfrak b^M+\mathfrak b^N` plus the secondary
`\varprojlim^1H^0` class.  The radial/Weyl theorem surface is the
`\Omega^{\mathrm{rad}}_{a,b}` obstruction, equivalently decorated PBW
Stokes for `D^\square_{a,b}`, not a bare homotopy slogan.  The larger
non-scalar Costello/QME theorem requires the filtered scalar projection,
finite row arrays, the primitive matrix `A^M c=-r^M`, transition
matrices, Roos compatibility, and a curved bulk-to-defect kernel.
```

### 2. `AGENTS.md:97`

Add the same facts as a new numbered agent rule after current rule 10 and
renumber the current Supremum rule if desired:

```markdown
11. The late 2026-04-30 Vol II cache predicates are binding control data:
    BMK one-pair pro-Matlis retract is not strict native all-window
    support-local current transfer; `\theta_3` requires a CE ancestor,
    scalar-zero Costello counterterm, or complete companion-face table plus
    `\Delta^1_{M,N}` and secondary `\varprojlim^1H^0`; radial/Weyl means the
    `\Omega^{\mathrm{rad}}` / signed-row / decorated PBW Stokes obstruction
    surface; larger non-scalar QME means scalar projection, finite row
    arrays, `A^M c=-r^M`, transition matrices, Roos compatibility, and a
    curved bulk-to-defect kernel.
```

The same factual insertion must be mirrored in `CLAUDE.md` in the same
commit because `AGENTS.md:5` requires no factual divergence.

### 3. `reconstitution/attack-heal-latest-2026-04-30.md:181-208`

Replace the "Precise Obstruction Classes" block with the current named
classes:

```markdown
- BMK native all-window current obstruction:
  `\operatorname{Ob}^{\Pi}_{\mathrm{BM}}`.
- Non-scalar graph/QME fixed-window obstruction:
  `A^M c=-r^M`, failure certified by `\ell A^M=0`,
  `\ell(r^M)\ne0`, plus `\mathfrak O^{ns}_n`.
- `\theta_3` Bianchi transport:
  `\Delta^1_{M,N}=-\pi_{M,N}\mathfrak b^M+\mathfrak b^N`,
  followed by the secondary `\varprojlim^1H^0` primitive-compatibility
  class.
- Radial all-`N` trace obstruction:
  `\Omega^{rad}_{a,b}=[(T_{a,b},E^+_{a,b})]\in\operatorname{coker}B_{a,b}`,
  failure exactly a signed row in `\ker B_{a,b}^*`.
```

### 4. `open-obligations.tex:498-526`

Patch the componentwise summary so it points to the detailed radial gate at
`open-obligations.tex:1410-1437`:

```tex
The remaining all-bidegree theorem is the exact radial obstruction theorem
\(\Omega^{\mathrm{rad}}_{a,b}=0\), equivalently the decorated PBW Stokes
identity for \(D^\square_{a,b}=C^+_{a,b}\partial_2\).  Failure is a signed
row \((\phi,-\lambda)\in\ker B_{a,b}^*\) with
\(\lambda(E^+_{a,b})-\phi(T_{a,b})\ne0\).
```

### 5. `claim-strength-ledger.tex:207-215`, `:300-308`, `:577-585`,
`:1238-1270`

Replace older `\mathcal C_{a,b}` / contracting-homotopy-only summaries by
the same `\Omega^{rad}` / `D^\square` / signed-row language.  The detailed
ledger row at `claim-strength-ledger.tex:987-1043` is already the model.

### 6. `CLAUDE.md:74` and `AGENTS.md:101-103`

When the Omega paragraph is next touched, append:

```markdown
Omega localization data are not QME data.  A brane-defect QME theorem must
still construct `K_{\Omega,N}` with
`d_{\mathrm{QME}}K_{\Omega,N}+\frac12[K_{\Omega,N},K_{\Omega,N}]_{\hbar_{\mathrm{QME}}}=0`,
after scalar projection, non-scalar classes, finite-window row data, and
centrality homotopies are supplied.
```

## Commands Run

- `sed -n` / `nl -ba` on `CLAUDE.md`, `AGENTS.md`, ecosystem invariants,
  the Vol II rectification skill, Vol II cache files, `open-obligations.tex`,
  `claim-strength-ledger.tex`, and current reconstitution reports.
- `rg` for compact-CY/CoHA/BKM/curve-VOA, Supremum, native `E_2`, BMK,
  Matlis, `\theta_3`, `\Delta^1`, radial, and non-scalar QME predicates.
- `git status --short` to observe concurrent dirty state.

No build was run.  This was a control-layer audit only.

## Files Changed

- `reconstitution/vol2-control-surface-propagation-audit-2026-04-30.md`
- `reconstitution/swarm-2026-04-30-agent-324-vol2-control-surface-propagation-audit.md`
