# Attack-Heal Latest Snapshot, 2026-04-30

## 09:57 SAST Critique Override

The active critique source is now
`materials/raw/2026-04-30-mixed-holomorphic-topological-strings-critique.pdf`,
created 2026-04-30 09:57:34 SAST, 1426 pages, raw SHA-256
`4d2c7af70e52c51faef63177cd1b7e2a0e54ae08d1b42228d3f78746d6640b21`.
The extracted text is
`materials/processed/2026-04-30-mixed-holomorphic-topological-strings-critique.txt`,
text SHA-256
`5e56a4a3fdb694d7de8bd24dcfe18b1f519b31c2a1b89229259415054fd0ca13`.

This supersedes the 09:43 and earlier control surfaces.  The detailed
ingestion, adversarial review, and next-swarm topology are recorded in
`reconstitution/critique-refresh-2026-04-30-0957-ingestion-and-swarm-plan.md`.

The new material adds a normal \(\Omega\)-background / stratified
factorization layer:

- the brane-preserving topological equivariance is \(s\mapsto\lambda_s s\)
  with \(t\) fixed, not literal rotation in the \((t,s)\)-plane;
- \(Q_\Omega=Q+\iota_{V_\Omega}\) and \(Q_\Omega^2=L_{V_\Omega}\) must
  be constructed in the mixed de Rham/Dolbeault category;
- nonzero normal modes become acyclic only after inverting their
  equivariant weights;
- off \(\varepsilon_1+\varepsilon_2=0\), the Hamiltonian bracket is
  line-valued or \(\hbar_\omega\)-weighted;
- the brane belongs in a stratified factorization algebra on \((X,L)\);
- protected traces and physical \(\Omega\)-large-\(N\) statements remain
  theorem surfaces requiring brane-defect QME and trace-state
  normalization.

## 09:43 SAST Critique Override

The active critique source is now
`materials/raw/2026-04-30-mixed-holomorphic-topological-strings-critique.pdf`,
created 2026-04-30 09:43:08 SAST, 1394 pages, raw SHA-256
`b2ad8fe6156b4d5141c574e8f52cc1368f3fcdd76e8ff384b16850f83e8c7aa4`.
The extracted text is
`materials/processed/2026-04-30-mixed-holomorphic-topological-strings-critique.txt`,
text SHA-256
`e05d28e561b04c4ffec170e4507562f8077d2d224d6cb2ab19326f50e8b2d6ad`.

This supersedes the 04:11 SAST, 923-page control surface.  The detailed
ingestion and next-swarm partition are recorded in
`reconstitution/critique-refresh-2026-04-30-0943-ingestion.md`.

The new architectural correction is:

- native theory: \(\mathbb R^2_{\mathrm{top}}\times
  \mathbb C^2_{\mathrm{hol}}\);
- native closed object: Hamiltonian BF / CE observables for
  \(\mathfrak h\ltimes P[1]\);
- native holomorphic object: a two-complex-dimensional holomorphic
  \(E_2\)-type factorization algebra on \(\mathbb C^2\), not a curve
  VOA;
- curve chiral, Zhu, and Vol-II \(SC_{\mathrm{ch,top}}\) structures
  enter only after a controlled \(\mathbb C\times\mathbb R\) reduction
  retaining \(z_2\)-modes or principal-part coefficients;
- \(\Omega\)-background localization is a new theorem surface for normal
  equivariant weights, not a proof of Costello QME or large-\(N\)
  physics.

Agent 157 control-surface refresh after the locally present Agent
136--151 and 153--154 evidence.  Reports 152, 155, and 156 are named in
the live log but absent from the checkout at this read.  Reports are
evidence, not authority; the local files and scoped verification commands
control.

## Corrected Stable Core

The active paper is the formal-local mixed holomorphic-topological SFT on
\[
  \mathbb R^2_{\mathrm{top}}\times
  \widehat{\mathbb C^2}_{0,\mathrm{hol}} .
\]
The holomorphic datum is the local unimodular symplectic surface
\[
  \left(\widehat{\mathbb C^2}_0,\Omega_{\mathrm{loc}}\right),
  \qquad \Omega_{\mathrm{loc}}=dz_1\wedge dz_2 .
\]
It supplies the Hamiltonian bracket, divergence-free Hamiltonian vector
fields, BV density, residue orientation, and cyclic trace density.  It is
not a compact Calabi-Yau threefold hypothesis.

Compact Calabi-Yau, CoHA, quintic, OSV/GV, Abel-Jacobi, MNOP, Vol III,
Igusa, Borcherds, and BKM material is external comparison only.  None of
it supports the local QME, Ext, cyclic, Weyl/Moyal, radial-parts, kernel,
or finite-window theorem surface without a separate matched-conventions
theorem.

## Evidence Window

| Agent | Local status |
|---|---|
| 136 | Repaired abstract/main/theorem-lane geometry to the local mixed HT surface; compact-CY imports quarantined. |
| 137 | Added local unimodularity dictionary: \(\Omega_{\mathrm{loc}}\), bracket, Hamiltonian vector fields, divergence, residue basis, open trace. |
| 138 | Repaired claim/open-obligation ledgers so compact-CY/CoHA lanes are external gates, not core prerequisites. |
| 139 | Replaced wavefront smoothing language by the obstruction class \(\eta^{\mathrm{reg}}_{n_0,M}\). |
| 140 | Built free indexed trace-diagram normal form; \(K_{4,4}\) is verified in the free diagram algebra; the signed-row obstruction surface remains open. |
| 141 | Replaced the finite-window tuple placeholder by row arrays with signs, scalar gates, row values, and truncation coefficients. |
| 142 | Localized the factorization-current appendix and current-kernel layer; full \(\mathcal D'_c(I)\) graph theorem remains obstructed. |
| 143 | Localized unreduced BV/QME scalar-shadow and balanced supertrace habitats; non-scalar graph classes remain outside the scalar projection. |
| 144 | Added a primitive-cone extension killing the wavefront residual only in an enlarged complex; original-complex exactness remains the obstruction. |
| 145 | Added order-two finite-window boundary rows; minimal full-equivariant order-two residual is zero; larger packages need actual rows. |
| 146 | Integrated local branch statuses into theorem lanes and claim ledger. |
| 147 | Added local Costello BV source fixture; sources support finite-scale BV/RG/QME vocabulary only, not current-valued \(D'_c\) graph theorems. |
| 148 | Defined the fixed-window non-scalar kernel complex and obstruction class \(\mathfrak o^{\mathrm{ns}}_{n,M}\). |
| 149 | Added local chiral/factorization taxonomy; no VOA, CoHA, compact chiral homology, or BKM object is asserted. |
| 150 | Report-only build/reference audit present but untracked; clean isolated draft build passed before later 153/154 edits. |
| 151 | Proved order-two bracket rows containing \(\alpha_{\mathrm{sc}}\) vanish because \(K^M_{\alpha_{\mathrm{sc}}}=0\). |
| 152 | Report absent; launch log says live kernel-contracting-homotopy lane. |
| 153 | Report present and staged; explicit zero scalar-contact row \(\alpha_{11}\) computed with \(\omega(z_1,z_1)=0\). |
| 154 | Report present and staged in final status; arbitrary \(\mathcal D'_c(I)\) graph kernels remain obstructed, regular/wavefront habitats remain the theorem-grade surface. |
| 155 | Report absent; launch log says live holomorphic-defect/VOA restriction lane. |
| 156 | Report absent; launch log says live genuine \(|\Gamma|_\hbar=2\) graph-weights lane. |

## Retired Stale Framing

The older snapshot centered compact-CY fixture completion as a leading
control surface.  That is stale for the core manuscript.  Compact source
fixtures may remain useful only for external matched-conventions
comparison lanes.  The active control surface is local: Hamiltonian
\(\mathbb C^2\) geometry, local BV/QME, defect current algebras,
finite-window Costello rows, regular-density and wavefront habitats, and
Weyl/Moyal radial computations.

## Top-Level Control Load Mandate

Future agents must load `CLAUDE.md`, `AGENTS.md`, this snapshot, and,
before any manuscript-proper writing or theorem-lane reconstitution,
`~/chiral-bar-cobar-vol2/.agents/skills/chriss-ginzburg-rectify/SKILL.md`.
The binding late predicates are the native \(\mathbb C^2\) holomorphic
\(E_2\) taxonomy; BMK one-pair pro-Matlis retract versus
\(\operatorname{Ob}^{\Pi}_{\mathrm{BM}}\); \(\theta_3\) with
\(\Delta^1_{M,N}\) and secondary \(\varprojlim^1H^0\); radial
\(\Omega^{\mathrm{rad}}\), decorated PBW Stokes, and signed-row
obstruction; and the non-scalar QME finite-window matrix, Roos, and
curved-kernel criterion.

## Proved Local Branches

- Local geometry/firewall: the core theorem surface is bound to
  \(\mathbb R^2_{\mathrm{top}}\times\widehat{\mathbb C^2}_{0,\mathrm{hol}}\);
  BCOV/Kodaira-Spencer language is convention source only.
- Local unimodularity: \(\Omega_{\mathrm{loc}}\), the Hamiltonian bracket,
  divergence-free Hamiltonian fields, residue basis, and open cyclic trace
  are recorded as the only Calabi-Yau input.
- Balanced scalar-contact branch: scalar shadows vanish on the stated
  balanced scalar-contact and balanced graph/counterterm habitats.
- Full-equivariant minimal finite-window branch: order \(0\), the
  one-cross scalar-contact row, the minimal codimension-two closure, and
  bracket rows containing \(\alpha_{\mathrm{sc}}\) have zero residual and
  require \(C_{1,M}=C_{2,M}=0\).
- Diagnostic zero row: the row \(\alpha_{11}\) with inputs
  \((z_1,z_1)\) has \(\omega(z_1,z_1)=0\) and zero class, if zero rows are
  retained by convention.
- Radial trace branch: the free indexed normal form verifies the
  \(K_{4,4}\) correction before finite-rank specialization; this is not an
  all-bidegree theorem.
- Clean isolated build/reference audit: Agent 150 reports a clean isolated
  draft build with no undefined references or multiply-defined labels at
  that time; this is not a final integrated `make pdf` after later edits.

## Constructed Habitats

- Local Hamiltonian current source and principal-part duality with
  \(\mathfrak h=\mathbb C[[z_1,z_2]]/\mathbb C\cdot1\).
- Algebraic coefficient-current kernel allowing arbitrary
  \(B\in\mathcal D'_c(I)\).
- Regular-density Costello graph habitat.
- Finite graphwise wavefront-admissible habitat with explicit wavefront,
  pushforward, scaling-degree, and collision-diagonal counterterm data.
- Primitive-cone extension for a compatible wavefront residual; this is an
  added cone, not exactness in the original regular-density quotient.
- Finite-window graph row arrays
  \(\mathsf A^{\mathrm{fw}}_{n,M}\), scalar gates \(S_{\alpha,M}\),
  row values \(R^{\mathrm{row}}_{\alpha,M}\), and truncation coefficients
  \(t^{M,N}_{\alpha\alpha'}\).
- Non-scalar kernel complex
  \[
    \mathcal K^\bullet_{\mathrm{ns},M}(I)
      =\ker\widehat\sigma_{\mathrm{sc},M}.
  \]
- Local chiral/factorization taxonomy separating holomorphic
  factorization, mixed topological currents, Weyl/Moyal brane algebra,
  and principal-part defect currents.
- Native holomorphic \(E_2\) surface on \(\mathbb C^2\):
  \(C^\bullet_{\mathrm{CE}}(\Omega_c^{0,\bullet}(B)\widehat\otimes
  (\mathfrak h\ltimes P[1]))\), with curve chiral/Zhu structures only
  after controlled reduction.

## Precise Obstruction Classes

- BMK native all-window current obstruction:
  \[
    \operatorname{Ob}^{\Pi}_{\mathrm{BM}}
    =
    (\operatorname{ob}_{\oplus},\operatorname{ob}_{I_\Pi},
     \operatorname{ob}_{\mathfrak h,\Pi},
     \operatorname{ob}_{\mathrm{collar},\Pi},
     \operatorname{ob}_{3,\Pi},\operatorname{ob}_{\mathrm{unif},\Pi}).
  \]
- Non-scalar graph/QME fixed-window obstruction:
  \(A^Mc=-r^M\), with failure certified by
  \(\ell A^M=0\) and \(\ell(r^M)\ne0\).  Inverse-limit closure requires
  transition matrices and vanishing of
  \[
    \mathfrak O^{\mathrm{ns}}_n
      =
      \bigl(([\mathfrak o^{\mathrm{ns}}_{n,M}])_M,\lambda_n\bigr).
  \]
- Windowwise non-scalar graph/QME cohomology class:
  \[
    \mathfrak o^{\mathrm{ns}}_{n,M}
      =[R^{\mathrm{ns}}_{n,M}]
      \in H^1(\mathcal K^\bullet_{\mathrm{ns},M}(I),d_{\mathrm{ns},M}).
  \]
- \(\theta_3\) Bianchi transport:
  \[
    \Delta^1_{M,N}=-\pi_{M,N}\mathfrak b^M+\mathfrak b^N,
  \]
  followed by the secondary \(\varprojlim^1H^0\)
  primitive-compatibility class after a primitive is chosen.
- Wavefront regular-density obstruction:
  \[
    \eta^{\mathrm{reg}}_{n_0,M}
      =[\mathfrak s^{\mathrm{WF}}_{n_0,M}]
      \in H^1(\mathcal C^{\bullet,\mathrm{reg}}_{w,M}(I)).
  \]
- Compatible primitive obstruction for finite windows and weights:
  \[
    \varprojlim\nolimits^1 H^0
  \]
  for the counterterm tower.
- Scalar-projection residual:
  \[
    H^1(\ker\widehat\sigma_{\mathrm{sc}},d_{\mathrm{QME}}).
  \]
- Radial all-\(N\) trace obstruction:
  \[
    \Omega^{\mathrm{rad}}_{a,b}
      =[(T_{a,b},E^+_{a,b})]\in\operatorname{coker}B_{a,b},
  \]
  equivalently decorated PBW Stokes
  \(D^\square_{a,b}=C^+_{a,b}\partial_2\); failure is exactly a signed row
  \((\phi,-\lambda)\in\ker B_{a,b}^*\) with
  \(\lambda(E^+_{a,b})-\phi(T_{a,b})\ne0\).
- Arbitrary \(\mathcal D'_c(I)\) graph obstruction: coincident singular
  labels such as \(B_1=B_2=\delta_{t_0}\) can violate Hormander
  transversality; extra renormalization data would be new theorem input.
- Vertex/OPE obstruction: a one-dimensional vertex algebra needs a
  separate complex-line or holomorphic-defect restriction theorem.

## Active Local Obligations

1. Decide the row convention for zero rows.  If zero rows are suppressed,
   \(\alpha_{11}\) is diagnostic only.
2. Supply any nonzero order-one scalar-zero row outside
   \(\alpha_{\mathrm{sc}}\), with \(K^M_\Gamma\), signs, scalar gate, and
   truncation data.
3. Populate genuine \(|\Gamma|_\hbar=2\) graph weights, CE-source faces,
   BV/convolution bracket rows, lower-counterterm terms, scalar gates, and
   truncation coefficients.
4. Compute \(\mathfrak o^{\mathrm{ns}}_{n,M}\).  If it vanishes
   windowwise, compute the Roos compatibility class for the counterterm
   tower.
5. Decide whether \(\eta^{\mathrm{reg}}_{n_0,M}\) is exact in the
   original distributional quotient complex; the primitive cone does not
   settle original-complex exactness.
6. Prove or import a current-compatible Costello theorem for the desired
   \(\mathcal D'_c\) graph/counterterm habitat, or keep the theorem at
   regular-density plus finite graphwise wavefront-admissible labels.
7. Construct the continuous bulk-to-defect kernel and the centrality and
   bracket homotopies needed for the non-scalar QME class.
8. Prove \(\Omega^{\mathrm{rad}}_{a,b}=0\) for every bidegree,
   equivalently the decorated PBW Stokes identity
   \(D^\square_{a,b}=C^+_{a,b}\partial_2\), or exhibit the signed-row
   obstruction \((\phi,-\lambda)\in\ker B_{a,b}^*\); until then keep
   the all-\(N\) Weyl/Moyal trace comparison relative to certified
   bidegrees and the exact signed-row obstruction theorem.
9. Prove a complex-line or holomorphic-defect restriction theorem before
   asserting a VOA or OPE-mode object.
9a. If using the refreshed critique's Dirac BRST curve chiral algebra,
    first construct the reduction datum
    \(\pi(t,s;z_1,z_2)=(t;z_1)\), retain \(z_2\)-coefficients or
    principal parts, prove \(Q_{\mathrm{BRST}}^2=0\), compute Zhu, and
    only then compare via Hochschild/HKR/CE-PV.
10. Treat compact-CY/CoHA/quintic/OSV/GV/Abel-Jacobi/MNOP/Vol III/Igusa/BKM
    only as external comparison lanes with target data, comparison maps,
    obstruction vectors, and null-homotopies.
11. Resolve absent Agent 152, 155, and 156 evidence before declaring the
    136--156 band closed.
12. Run a final main-thread build from a clean aux state after integration;
    current evidence is scoped checks plus isolated draft builds.
