# Swarm 2026-04-30 Agent 324: Vol II Control Surface Propagation Audit

Report-only audit.  No manuscript TeX edited.  No build run.

## Claim Attacked

The post-update control layer might still allow Vol II anti-patterns to
propagate into this repo: compact-CY/CoHA/BKM transfer, curve-VOA import,
loss of Supremum Discipline, collapse of the native `\C^2` `E_2`
taxonomy, BMK/Matlis conflation, missing `\theta_3` `\Delta^1` transport,
old radial obstruction language, or overclaiming a non-scalar Costello/QME
theorem.

## Verdict

The dangerous false transfers are blocked.  `CLAUDE.md`, `AGENTS.md`,
`open-obligations.tex`, and `claim-strength-ledger.tex` all state the local
mixed HT surface, the compact-CY/CoHA/BKM quarantine, and the curve-VOA
reduction gate.

The late-day control predicates are fully visible in the open/ledger layer
but only partially visible in the top-level agent files:

- BMK one-pair pro-Matlis split: visible in `open-obligations.tex:274-309`
  and `claim-strength-ledger.tex:319-336`, `:641-684`; absent by name from
  `CLAUDE.md` and `AGENTS.md`.
- `\theta_3` and `\Delta^1_{M,N}`: visible in
  `open-obligations.tex:790-952` and `claim-strength-ledger.tex:512-546`,
  `:932-985`; absent by name from `CLAUDE.md` and `AGENTS.md`.
- Radial `\Omega^{rad}` obstruction: detailed surfaces are present at
  `open-obligations.tex:1410-1437` and `claim-strength-ledger.tex:987-1043`;
  older summary rows still need propagation.
- Non-scalar QME finite-window criterion: visible at
  `open-obligations.tex:542-700`, `:704-788`, `:1113-1217` and
  `claim-strength-ledger.tex:915-985`, `:1295-1407`; top-level files are
  generic.

## Evidence Anchors

- Top-level false-transfer firewall:
  `CLAUDE.md:31-56`, `CLAUDE.md:77-88`,
  `AGENTS.md:60-64`, `AGENTS.md:83-96`, `AGENTS.md:258-269`.
- Supremum Discipline:
  `CLAUDE.md:119-141`, `AGENTS.md:105-117`,
  `claim-strength-ledger.tex:632-638`.
- Native `\C^2` `E_2` taxonomy:
  `CLAUDE.md:39-56`, `AGENTS.md:31-49`,
  `open-obligations.tex:224-254`,
  `claim-strength-ledger.tex:641-705`.
- External comparison gate:
  `open-obligations.tex:1462-1501`,
  `claim-strength-ledger.tex:1410-1432`.

## Exact Patch Targets

No target below was patched.

1. `CLAUDE.md:56`: insert a "Late 2026-04-30 control predicates" paragraph
   naming BMK `Ob^{\Pi}_{BM}`, `\theta_3`/`\Delta^1`, radial
   `\Omega^{rad}` / `D^\square`, and the non-scalar QME matrix criterion
   `A^M c=-r^M`.

2. `AGENTS.md:97`: add the same facts as an agent rule near the Omega and
   Supremum rules.  Mirror the facts in `CLAUDE.md`; `AGENTS.md:5`
   requires no factual divergence.

3. `reconstitution/attack-heal-latest-2026-04-30.md:181-208`: replace the
   older obstruction list with the current named classes:
   `Ob^{\Pi}_{BM}`, `A^M c=-r^M`, `\Delta^1_{M,N}`, and
   `\Omega^{rad}_{a,b}`.

4. `open-obligations.tex:498-526`: update the componentwise quantum
   surface summary to the detailed radial obstruction now present at
   `open-obligations.tex:1410-1437`.

5. `claim-strength-ledger.tex:207-215`, `:300-308`, `:577-585`, and
   `:1238-1270`: replace older radial `\mathcal C_{a,b}` /
   contracting-homotopy summaries by `\Omega^{rad}` / `D^\square` /
   signed-row language, matching `claim-strength-ledger.tex:987-1043`.

6. `CLAUDE.md:74` and `AGENTS.md:101-103`: when the Omega warning is next
   touched, append that Omega localization does not construct
   `K_{\Omega,N}` or solve
   `d_QME K + 1/2[K,K]_{\hbar_QME}=0`; scalar projection, non-scalar
   classes, finite rows, centrality homotopies, and the curved
   bulk-to-defect kernel remain required.

## Result

The live control layer is mathematically usable.  The residual task is
propagation hygiene: promote the late-day cache predicates into
`CLAUDE.md`, `AGENTS.md`, and the stale summary rows so the next agent sees
the same theorem gates before reading the detailed ledgers.

## Files Changed

- `reconstitution/vol2-control-surface-propagation-audit-2026-04-30.md`
- `reconstitution/swarm-2026-04-30-agent-324-vol2-control-surface-propagation-audit.md`
