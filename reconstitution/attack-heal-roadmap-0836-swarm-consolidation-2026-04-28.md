# Corrected 08:36 Roadmap Attack-Heal Swarm Consolidation

Date: 2026-04-28.

Canonical input:

- `materials/raw/2026-04-28-0836-whitepaper-critical-analysis.pdf`.
- `materials/processed/2026-04-28-0836-whitepaper-critical-analysis.txt`.

The erroneous 09:03 artifact is deleted and non-authoritative. Earlier
roadmap findings depending on it are archival only.

## Executive Decision

The clean plan has the right reduced mathematical direction, but it is
not yet an executable theorem-construction plan. The surviving core is
the reduced local CE/PV spine with scalar-reduced Hamiltonian brane
sector, Matlis residue-current cotangent module, PBW one-psi shadow,
formal Moyal coefficients with correct parity, reduced Wick
coefficients, finite-window weighted Tate control, scalar contact/QME
candidate, and cross-volume no-transfer firewall.

The following claims remain open theorem lanes, not paper-level
theorems:

- full `Z^{P_0}_{fact}` for the open factorization algebra;
- full open BV/factorization algebra identification;
- conilpotent Koszul duality for the full formal Hamiltonian algebra;
- principal parts as an `R`-submodule or quotient wording;
- unconditional all-`N` radial-parts theorem;
- normalized-Moyal "no `\hbar^2` correction";
- Costello graph theorem from reduced Wick diagrams;
- exactly-three QME classification.

## Agent Results

| Agent | Lens | Integrated result |
|---|---|---|
| C01 Lovelace | critique fidelity and source correction | Valid. Old B-wave artifacts and older critique rows can still leak deleted-source or stale instructions. The 08:36 source must be canonical; older copies are archival. |
| C02 Parfit | roadmap executability | Valid. The clean plan is mostly triage. It needed a roadmap index, criticism triage ledger, resolution blueprint, and proof/source-owner matrix. |
| C03 Ampere | full `Z^{P_0}_{fact}` / open BV | Valid. Reduced CE/PV survives; full center, full open BV, Tate HKR, compact-support current shifts, interval factorization, and bubble-sort centrality remain open. |
| C04 Helmholtz | Koszul/Tate/Matlis/Rees/one-psi | Valid. Conilpotent Koszul must be restricted; Matlis must be residue-current/C-linear; Rees/Fourier is a lattice-label comparison; one-psi proof is not full open BV. |
| C05 Kepler | quantum/radial/Moyal/stable `N` | Valid. Normalized Moyal brackets generally have `\hbar^2` corrections. All-`N` radial parts, connected projection, and stable target remain conditional. |
| C06 Huygens | Costello/weighted/QME | Valid. Reduced Wick coefficients are not Costello BV graphs. Weighted regulator results are finite-window. QME mechanisms are not classified. |

## Root-Cause Attacks

### R1. Roadmap Authority Was Ambiguous

The older platonic-ideal plan, B-wave consolidation, G6 roadmap, and
W32 edit plan contain useful evidence but can drive false closure. They
are superseded or archival under `roadmap-index-2026-04-28.md`.

Heal:

- Create and use `roadmap-index-2026-04-28.md`.
- Label old source/provenance rows archival.
- Treat `platonic-clean-paper-plan-2026-04-28.md` as triage base, not
  as a closed proof blueprint.

### R2. Reduced and Full Centers Were Collapsed

The reduced local `PV` convention was still written with
`Z^{P_0}_{fact}` notation in active manuscript control files. This is
the exact type collapse the critique attacks.

Heal:

- Use `Z^{P_0}_{red}(A):=\mathrm{PV}_{red}(A)` for the reduced model.
- Reserve `Z^{P_0}_{fact}` for a constructed full factorization center.
- Do not state centrality against arbitrary open observables until the
  full open BV/factorization algebra and homotopies are written.

### R3. The Interval Current Model Needs a Degree Repair

The critique and C03 identify a mismatch between top-degree
compact-support cohomology and prose treating degree-zero test
functions as the surviving point class.

Heal:

- Choose the shifted compact-support complex or density/current
  convention.
- Prove integration/local constancy on intervals.
- Recompute CE degrees, brackets, and orientation signs before using the
  interval factorization theorem.

### R4. Tate HKR and Factorization Functoriality Are Not Citation Pins

The completed Tate current algebra requires finite-window and topology
proofs. Classical HKR citations do not by themselves close the theorem.

Heal:

- Prove finite-window HKR and pass to the limit with exactness control,
  or keep only finite-window/reduced stalk statements.
- Prove completed symmetric cosheaf functoriality before claiming
  factorization algebra equivalence.

### R5. Matlis Is Residue-Current, Not an `R`-Submodule

Bare `Ann(C.1)`, quotient-by-constant language, and polynomial
raising/lowering slogans are not safe.

Heal:

- Use `\operatorname{Ann}_{res}(\mathbb C\cdot 1)` in
  `H^2_{\mathfrak m}(\mathbb C[[z_1,z_2]])`.
- State a C-linear residue-current coadjoint module.
- Keep polynomial descendants as PBW/Rees shadows unless a different
  target and action are proved.

### R6. Quantum and Radial Statements Need Conditional Boundaries

The normalized bracket has even corrections; the first correction is
generally nonzero. The all-`N` radial-parts theorem and connected
projection are still not proved by finite-rank scripts.

Heal:

- Separate raw Moyal commutator and normalized Hamiltonian bracket.
- Choose a stable coefficient object supporting `\hbar N` and
  `\hbar^2N^3`.
- Prove `Rad_N`, image, Capelli correction, and connected extraction, or
  state the comparison conditionally.

### R7. Costello and QME Are Open Realization Problems

Reduced Wick coefficients are algebraic computations. They do not
construct a Costello heat-kernel graph theorem. QME cancellation
mechanisms are candidates until the obstruction complex is computed.

Heal:

- Rename figures/captions to reduced Wick coefficient diagrams where
  appropriate.
- Build a Costello realization only with fields, kernels, propagators,
  counterterms, scale limits, and coefficient matching.
- Compute QME signs and obstruction cohomology before any
  classification language.

## Stable Core

Safe to build around:

- reduced local CE/PV generator identity;
- scalar-reduced connected Hamiltonian PBW sector;
- Matlis residue-current coadjoint module;
- one-psi PBW shadow as a selected sector;
- formal Weyl/Moyal coefficient algebra with correct normalization;
- reduced first/third Wick coefficients;
- finite-window weighted Tate regulator control;
- algebraic scalar contact/QME candidate;
- explicit no-transfer firewall to compact CY, Vol III, BKM,
  Borcherds, and Igusa claims.

## Required Next Manuscript Edits

1. Patch front matter and ledgers to the reduced core:
   `abstract.tex`, `theorem-lanes.tex`, `claim-strength-ledger.tex`,
   and `open-obligations.tex`.
2. Replace reduced `Z^{P_0}_{fact}` notation by `Z^{P_0}_{red}`.
3. Replace bare `Ann(C.1)` by residue-current notation.
4. Correct normalized-Moyal `\hbar^2` language.
5. Rename Costello graph claims as reduced Wick coefficient claims unless
   the Costello construction is supplied.
6. Split undefined hybrid statuses into defined status rows.

## New Control Artifacts

- `reconstitution/roadmap-index-2026-04-28.md`.
- `reconstitution/criticism-triage-2026-04-28.md`.
- `reconstitution/resolution-blueprint-2026-04-28.md`.
- `reconstitution/proof-source-owner-matrix-2026-04-28.md`.

These artifacts make the roadmap assignable. They do not close the hard
theorems.
