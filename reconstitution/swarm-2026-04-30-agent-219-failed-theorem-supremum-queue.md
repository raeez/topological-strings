# Agent 219 Report -- Failed-Theorem Supremum Queue And Validator Audit

Date: 2026-04-30.

Writable scope obeyed: only
`reconstitution/failed-theorem-surface-supremum-queue-2026-04-30.md` and
this report. No manuscript or script edits.

## Instruction Stack Loaded

- `CLAUDE.md`.
- `AGENTS.md`.
- `/Users/raeez/ecosystem/skills/claude-code/attack-heal-swarm-plugin/skills/attack-heal-swarm/SKILL.md`.
- `/Users/raeez/ecosystem/skills/claude-code/attack-heal-swarm-plugin/skills/attack-heal-swarm/references/protocol.md`.
- `/Users/raeez/chiral-bar-cobar-vol2/.agents/skills/chriss-ginzburg-rectify/SKILL.md`.
- Supporting harness excerpts: `~/ecosystem/INVARIANTS.md` section IV and
  `~/ecosystem/AGENTS-HARNESS.md` section VIII.

## Files Audited

- `main.tex`.
- `theorem-lanes.tex`.
- `open-obligations.tex`.
- `claim-strength-ledger.tex`.
- `appendix-unreduced-bv-qme.tex`.
- `appendix-radial-parts-moyal.tex`.
- `scripts/finite_window_graph_array.py`.
- `scripts/check_derived_intersection_N_extended.py`.
- `scripts/check_moyal_coefficients.py`.
- `scripts/quantum_shear_primitive_search.py`.
- `scripts/quantum_shear_universal_formula.py`.
- `scripts/quantum_shear_trace_diagram_normal_form.py`.

## Output

Primary queue written:
`reconstitution/failed-theorem-surface-supremum-queue-2026-04-30.md`.

The queue records nineteen still-visible failed theorem surfaces, with
anchors and the strongest true target for each: construct the missing data, or
prove the exact obstruction theorem.

## Required Surfaces Confirmed

| Surface | Status | Supremum target |
|---|---|---|
| Non-scalar QME | Still open beyond the minimal full-equivariant finite-window branch. | Construct scalar chain projection, closed row table, finite-window counterterms, truncation identities, and zero Roos class; or prove `(([o^ns_M])_M,lambda)`. |
| `theta3` primitive/companion table | Current package remains a one-row obstruction. | Supply CE ancestor, local counterterm primitive, or complete source-face companion table; otherwise prove `ell_theta3(E_theta3)=1`. |
| BM all-window transfer | Arity-two finite-window data present; strict all-window transfer not constructed. | Build uniform BM transfer with moment/collar/ternary/uniform estimates; or prove `Ob_BM^infty` nonzero. |
| Radial all-bidegree homotopy/cokernel | Finite and edge certificates present; all-bidegree theorem absent. | Construct all-bidegree `K_{a,b}`/splitting for `C_{a,b}`, or produce a left-cokernel functional. |
| `Omega` finite-window habitat | Habitat data exist; QME proof absent. | Build `R_Omega^{N,M}` package, scalar projection, non-scalar residual kill, primitive transport, and basic centrality homotopies; or prove the displayed obstruction classes. |
| Native `E_2` reduction gates | Native object is still `C^2` factorization, not a curve VOA. | Construct `R_CxR` and then BRST/Zhu data; or prove the reduction obstruction vector. |

## Attack Ledger

1. Claim attacked: "native `E_2` data imply curve OPE/VOA/Zhu."
   Failure mode: false geometry transfer. `main.tex`, `theorem-lanes.tex`, and
   the ledger keep `C^2` factorization native and require `R_CxR`.
   Recommendation: keep as failed surface until the reduction datum and
   anomaly/pairing comparison are constructed.

2. Claim attacked: "finite-window BM rows imply all-window BM."
   Failure mode: inverse-limit uniformity, collar control, and ternary
   compatibility are not supplied.
   Recommendation: target `Ob_BM^infty` exactly.

3. Claim attacked: "balanced/minimal scalar vanishing solves non-scalar QME."
   Failure mode: scalar shadow vanishes only on named habitats; larger graph
   packages need actual rows, signs, source faces, scalar gates, primitives,
   and transport matrices before `H^1` exists.
   Recommendation: do not promote beyond the constructive finite-window
   criterion.

4. Claim attacked: "`theta3` is healed by naming a companion."
   Failure mode: the current data contain no primitive and no complete
   companion-face table. A name without differential, scalar-zero check,
   source-face closure, and transport is not a counterterm.
   Recommendation: retain `theta3` as exact one-row obstruction until one of
   the three accepted payload types is actually supplied.

5. Claim attacked: "radial finite certificates prove all-`N` Moyal trace."
   Failure mode: the finite and free-diagram scripts verify windows and
   finite cokernel data; they do not construct a functorial all-bidegree
   homotopy.
   Recommendation: target moment-ideal primitives for all `a,b,N`, or the
   left-cokernel obstruction.

6. Claim attacked: "`Omega` localization solves QME."
   Failure mode: localization supplies coefficient ring, `Q_Omega`, normal
   contractions, and branch normalization; scalar chain projection,
   non-scalar counterterms, Roos compatibility, and centrality homotopies are
   separate.
   Recommendation: keep finite-window habitat and QME vanishing separate.

7. Claim attacked: "ordinary scalar-reduced `gl_N` QME obstruction vanishes."
   Failure mode: `hbar N[bar c]` is nonzero in the scalar-reduced branch.
   Recommendation: use central character/source replacement, balanced
   supertrace, or prove the closed-exchange leading class condition.

## Theta3 Companion Validator Audit

Source audited: `scripts/finite_window_graph_array.py:922-1589`.

Positive checks in the current repair concept:

- The current `theta_3_current_finite_row_subcomplex` is forced to remain
  unsolved.
- Primitive payloads without a differential entry `dC=-E` are rejected.
- Exact CE-ancestor and local-counterterm fixtures are accepted only when
  support/locality, scalar-zero projection, transport identity, and zero Roos
  class are present.
- One-face companion data are rejected.
- A complete companion-face fixture is accepted only with nonempty faces,
  signed residual zero, scalar-zero faces, `v` truncation/cocycle data, and
  codimension-two closure.
- The accepted companion case is correctly described as changing the
  residual vector, not as producing a primitive.

Adversarial gaps:

1. The complete companion payload is an interface fixture. The validator does
   not derive the companion faces from the actual labelled CE/source-face
   differential of the graph package.

2. Scalar-zero is a supplied field on each face. The validator does not
   recompute `widehat sigma_sc` from marker tensors and cyclic supertrace.

3. Signed coordinate verification checks equality between residual
   coefficients and CE coefficients in the supplied table. It does not prove
   that the supplied table is the full codimension-two boundary table.

4. The truncation check is an abstract `v`-cocycle flag in the companion
   payload. It is not derived from actual window projection matrices.

5. Companion cancellation bypasses the primitive Roos class by changing the
   residual. A theorem is still needed that the added companion rows are
   legitimate source faces in the same native finite-window habitat, not
   extraneous cancelling rows.

6. Locality and wavefront/current support for companion faces are not
   recomputed. They are represented by payload booleans.

Verdict: the validator repair is conceptually sound as an interface gate and
as a guard against the previous weak repair. It is not a theorem that the
`theta3` obstruction is healed. The current manuscript/script state should
continue to record `theta3` as unsolved until the actual CE companion table or
primitive is supplied from the graph package.

## Residual Risk

The queue is a surface audit, not a proof repair. It does not decide whether
any obstruction class vanishes except where the current manuscript/scripts
already give a theorem or finite certificate. In particular, `(7,7)` in the
radial free-diagram basis remains computationally open, not a nonzero
obstruction.

## Files Changed

- Added `reconstitution/failed-theorem-surface-supremum-queue-2026-04-30.md`.
- Added `reconstitution/swarm-2026-04-30-agent-219-failed-theorem-supremum-queue.md`.

No build was run; no TeX or script source was modified.
